import { ResultsClient } from "@/components/organisms/pages/results";

export const metadata = {
  title: "Results — FixAnything",
  description: "Matched local professionals ranked by rating and proximity.",
};

export default function ResultsPage() {
  return <ResultsClient />;
}
